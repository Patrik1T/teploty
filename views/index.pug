doctype html 
html
    head
        title ToDoList
        link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css')
        script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js')
    body
        header.bg-success.text-white.text-center.p-3
            h1.text-center.text-white #{nadpis}

        main.container
          table(class="table table-striped")
             thead 
               tr
                  tr.bg-dark.text-light  
                      th Místo měření
                      th V kolik bylo měření
                      th Den
                      th MIN teplota
                      th MAX teplota
                      th Průměrná teplota
                      th Datum poslední teploty
               tbody 
                  
                    tr 
                        td #{teploty.Mereni}     
                        td #{teploty.obor}
                        td #{teploty.den}
                        td #{teploty.min}
                        td #{teploty.max}
                        td #{teploty.prumer}
                        td #{teploty.datum}
                        
        footer.bg-dark.text-light.text-center.p3
        p &copy; 2023 - IT2 - Patrik tomastik
        p
           a(href="/") Úvodní stránka
        p 
           a(href="/")  O aplikaci
           script(src='https://code.jquery.com/jquery-3.4.1.slim.min.js')
           script(src='https://getbootstrap.com/docs/4.4/dist/js/bootstrap.bundle.min.js')
